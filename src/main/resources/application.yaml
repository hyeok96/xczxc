spring:
  datasource:
    url: jdbc:mysql://localhost:3306/first_project?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: dltkdgur12
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect